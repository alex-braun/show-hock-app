
import { lt } from 'show-hock-app/helpers/lt';
import { module, test } from 'qunit';

module('Unit | Helper | lt');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lt([42]);
  assert.ok(result);
});

