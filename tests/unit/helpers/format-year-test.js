
import { formatYear } from 'show-hock-app/helpers/format-year';
import { module, test } from 'qunit';

module('Unit | Helper | format year');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatYear([42]);
  assert.ok(result);
});

