
import { noParens } from 'show-hock-app/helpers/no-parens';
import { module, test } from 'qunit';

module('Unit | Helper | no parens');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noParens([42]);
  assert.ok(result);
});

