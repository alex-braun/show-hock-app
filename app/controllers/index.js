import Ember from 'ember';

export default Ember.Controller.extend({

  // init() {
  //   let len = Ember.computed.alias('model');
  //   console.log(len);
  //   .data.event.length;
  //   let randArray = [];
  //   for (let i = 0; i < 4; i++) {
  //     randArray.push(Math.floor(Math.random()*(len-0)));
  //   }
  //   return this.set('randArray', randArray);
  // }
});
