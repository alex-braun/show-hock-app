
import { formatMon } from 'show-hock-app/helpers/format-mon';
import { module, test } from 'qunit';

module('Unit | Helper | format mon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatMon([42]);
  assert.ok(result);
});

