
import { formatWeekday } from 'show-hock-app/helpers/format-weekday';
import { module, test } from 'qunit';

module('Unit | Helper | format weekday');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatWeekday([42]);
  assert.ok(result);
});

