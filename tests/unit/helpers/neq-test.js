
import { neq } from 'show-hock-app/helpers/neq';
import { module, test } from 'qunit';

module('Unit | Helper | neq');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = neq([42]);
  assert.ok(result);
});

