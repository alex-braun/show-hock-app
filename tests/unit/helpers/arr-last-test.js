
import { arrLast } from 'show-hock-app/helpers/arr-last';
import { module, test } from 'qunit';

module('Unit | Helper | arr last');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrLast([42]);
  assert.ok(result);
});

