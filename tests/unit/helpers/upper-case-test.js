
import { upperCase } from 'show-hock-app/helpers/upper-case';
import { module, test } from 'qunit';

module('Unit | Helper | upper case');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = upperCase([42]);
  assert.ok(result);
});

