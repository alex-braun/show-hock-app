
import { prevCurr } from 'show-hock-app/helpers/prev-curr';
import { module, test } from 'qunit';

module('Unit | Helper | prev curr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = prevCurr([42]);
  assert.ok(result);
});

