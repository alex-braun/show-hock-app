
import { formatDay } from 'show-hock-app/helpers/format-day';
import { module, test } from 'qunit';

module('Unit | Helper | format day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDay([42]);
  assert.ok(result);
});

