{{!-- <div class='region-search-main-listing'> --}}

  <div class='search-page-results artist'>
    <div class='results-header artist'>
      <h4>ARTISTS</h4>
      {{#if (gt model.artist.meta.total_entries 10)}}
        <a href='javascript:;'{{action 'artistSearch' accessArtistParams.param}}><h5 class='see-more'>See More</h5></a>
      {{/if}}
    </div>
    {{#each model.artist.artist as |artist|}}
      {{search-results-artist artist=artist
                              goToArtist='goToArtist'}}
    {{/each}}
  </div>


  <div class='search-page-results'>
    <div class='results-header region'>
    <h4>REGIONS</h4>
      {{#if (gt model.region.meta.total_entries 10)}}
      <a href='javascript:;'{{action 'regionSearch' accessArtistParams.param}}><h5 class='see-more'>See More</h5></a>
      {{/if}}
    </div>
  {{#each model.region.location as |region|}}
    {{search-results-region region=region
                            goToRegion='goToRegion'}}
  {{/each}}
  </div>

  <div class='search-page-results'>
    <div class='results-header venue'>
      <h4>VENUES</h4>
      {{#if (gt model.venue.meta.total_entries 10)}}
        <a href='javascript:;'{{action 'venueSearch' accessArtistParams.param}}><h5 class='see-more'>See More</h5></a>
      {{/if}}

    </div>
  {{#each model.venue.venue as |venue|}}
    {{search-results-venue venue=venue
                           goToVenue='goToVenue'}}
  {{/each}}
  </div>

{{!-- </div> --}}


  {{!-- <ul class="event-listings row-fluid block-grid-3">
    {{#each model.location as |region|}}
        {{region-search-detail regionSelect=region}}
    {{/each}}
  </ul> --}}
